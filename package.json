{
  "name": "cronica-de-aethelgard",
  "version": "1.0.42",
  "description": "RPG Narrativo de Escolhas, Consequências, Dragões e Geopolítica Medieval 100% Offline com Atualização Automática via GitHub Pages",
  "main": "index.html",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.js",
    "cap:sync": "npm run prepare:web && npx cap sync android",
    "build:apk": "npm run prepare:web && npx cap sync android && cd android && ./gradlew assembleDebug",
    "deploy:pages": "echo 'Deploy handled by GitHub Actions'"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "author": "Arena.ai Agent Mode",
  "license": "MIT"
}